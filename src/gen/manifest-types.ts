/**
 * Minimal stub committed to keep `bun run typecheck` self-contained on a fresh clone.
 * AssetPack overwrites this file with the full generated version during `bun run dev` or `bun run build`.
 */

export type BundleName = 'loops' | 'main' | 'preload'
